# Database Relations

OrbitWork allows linking two databases.

1. Create a column property type "Relation".
2. Select the target database.
3. (Optional) Toggle "Show on other database" for bidirectional linking.

*Tip: Use Rollups to aggregate data from related rows (e.g., sum of sales).*

